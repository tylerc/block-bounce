== Main Game (block.rb)
* Powers don't appear at random intervals
* Powers don't die when they go offscreen
* Only one power is possible to get (because of testing)
* Powers should spawn when blocks are destroyed, not when touched

== Level Editor (level.rb)

== Level Properties Editor (properties.rb)

== Sprite Editor (sprite.rb)
(tool is discontinued... but still useful, continue to track?) - No
